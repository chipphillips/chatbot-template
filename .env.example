AI_GATEWAY_API_KEY=

# Optional Supabase runtime persistence.
# Keep service role keys server-only. Do not expose service role keys to the browser.
SUPABASE_PERSISTENCE_ENABLED=false
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Temporary single-user runtime owner for server-side persistence.
# Replace with authenticated user/session wiring before broader use.
SUPABASE_RUNTIME_USER_ID=
