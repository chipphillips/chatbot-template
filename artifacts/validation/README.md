# Validation Evidence

`founder-console-validation.json` is the current machine-readable receipt. It intentionally distinguishes passed checks from checks that were blocked or not run. A future agent must update rather than overwrite this truthfulness boundary.
